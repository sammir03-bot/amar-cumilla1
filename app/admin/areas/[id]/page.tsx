import {notFound,redirect} from 'next/navigation';
import {requireStaff} from '../../../../lib/supabase';
import ActionForm from '../../../../components/action-form';
import {saveArea} from '../../actions';
export default async function AreaEditor({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{saved?:string}>}){const {db,role}=await requireStaff();if(role!=='admin')redirect('/admin');const {data:a,error}=await db.from('cumilla_areas').select('*').eq('id',(await params).id).maybeSingle();if(error)throw error;if(!a)notFound();return <><h1>{a.name}</h1>{(await searchParams).saved&&<p role="status">সংরক্ষণ হয়েছে।</p>}<ActionForm action={saveArea}><input type="hidden" name="id" value={a.id}/><input type="hidden" name="version" value={a.updated_at}/><label>নাম<input name="name" required maxLength={180} defaultValue={a.name}/></label>{[['description','পরিচিতি'],['villages','গ্রাম ও ওয়ার্ড'],['institutions','শিক্ষাপ্রতিষ্ঠান ও গুরুত্বপূর্ণ স্থান'],['services','জনসেবার তথ্য']].map(([key,label])=><label key={key}>{label}<textarea name={key} rows={6} maxLength={20000} defaultValue={a[key]}/></label>)}<label>তথ্যসূত্রের লিংক<input type="url" name="source_url" defaultValue={a.source_url??''}/></label><label className="check"><input type="checkbox" name="verified" defaultChecked={!!a.verified_at}/>আমি তথ্য যাচাই করেছি</label><label className="check"><input type="checkbox" name="published" defaultChecked={a.published}/>মূল ওয়েবসাইটে প্রকাশ করুন</label></ActionForm></>;}
